global:
  scrape_interval: 5s

rule_files:
  - /etc/prometheus/prometheus-rules.yml

scrape_configs:
  - job_name: "llm"
    static_configs:
      - targets: ["app:8080"]
        labels:
          service: "llm"
