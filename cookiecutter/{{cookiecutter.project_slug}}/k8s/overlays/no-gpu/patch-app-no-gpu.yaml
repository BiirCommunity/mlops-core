apiVersion: apps/v1
kind: Deployment
metadata:
  name: app
  namespace: {{ cookiecutter.namespace }}
spec:
  template:
    spec:
      containers:
        - name: app
          resources:
            requests:
              memory: 4Gi
              cpu: "2"
            limits:
              memory: 16Gi
              cpu: "8"
